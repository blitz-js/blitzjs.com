---
title: blitz db
sidebar_label: blitz db
---

<!-- prettier-ignore-start -->
:::caution
This command is now deprecated in favor of using the
`blitz prisma` command. The `blitz db seed` command still works until
Prisma finishes adding their native seeding functionatily.
:::
<!-- prettier-ignore-end -->

### `blitz db seed` {#blitz-db-seed}

This will run `db/seeds.ts` script. This is often used to quickly setup
the development environment. For examples check
[seeds docs](database-seeds)
